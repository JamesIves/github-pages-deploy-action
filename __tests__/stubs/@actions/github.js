// CJS stub for @actions/github (ESM-only in v3+)
// Provides a minimal context object matching what the test environment needs.
// Note: constants.ts accesses github.context.payload at module load time.
module.exports = {
  context: {
    payload: {}
  }
}
